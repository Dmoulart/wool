add :: first, second -> f32
{ 
  first + second + 1 
}

/*
test_float_coercion :: {
  b := 1 
  c := 2

  if (c == 1.5) c else b // should coerce b and c to float !
}
*/

/*
// the problematic propagation case
a := 1.5 + (2 + 3 + (4 - 5))
*/

/*
a :: 1

b :: 2

// (add is a virtual (generic) function as long as it contains a non terminal type)
add :: first, second ->
{ 
  first + second 
}

// (main is always an actual function)
main :: -> {
  sum := add(a, b)

  sum
}

// (an actual function types should always be resolved to terminal types)
*/